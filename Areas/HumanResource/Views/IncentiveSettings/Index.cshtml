@model IEnumerable<Standus_5_0.Areas.HumanResource.Models.IncentiveSetting>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/HumanResource/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Incentive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AddnIncentive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeDetails)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Incentive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AddnIncentive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeDetails.Discriminator)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmployeeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmployeeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmployeeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
